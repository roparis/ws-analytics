# Implementation Plans

Generated by the `improve` skill on 2026-08-15, against commit `d1d2640` on
branch `WSA-009`.

Execute in the order below unless the dependency notes say otherwise. Each
executor: **read the plan fully before starting**, run its drift check first,
honour its STOP conditions, and update your row in the table when done.

Every plan is self-contained. You do not need to read the others, this README,
or any audit notes to execute one — except where a plan's "Depends on" row says
otherwise.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| [001](001-ci-workflow.md) | Run the existing checks on every push and PR | P1 | S | — | TODO |
| [002](002-merge-characterization-tests.md) | Cover `merge.ts` with characterization tests | P1 | M | — | TODO |
| [003](003-invariants-in-production.md) | Run the data-invariant checks in every build, show results in the UI | P1 | M | — | TODO |
| [004](004-preserve-unparseable-sources.md) | Stop deleting a source's raw text when it fails to re-parse | P1 | S | — | TODO |
| [005](005-gate-uploader-on-hydration.md) | Close the mid-hydration window that deletes saved files | P1 | S | — | TODO |
| [009](009-local-calendar-dates.md) | Derive calendar dates from the local clock, not UTC | P1 | M | soft: 001 | TODO |
| [013](013-agents-domain-knowledge.md) | Give `AGENTS.md` the domain knowledge that makes this repo hard | P1 | S | — | TODO |
| [010](010-price-history-partial-failure.md) | Stop discarding good price history on a partial failure | P2 | S | — | TODO |
| [011](011-closing-writeoff-date.md) | Date a pool's closing write-off to the event that closed it | P2 | S | — | TODO |
| [012](012-mis-scaled-price-parse.md) | Reject a mis-scaled price instead of reading it as a fraction | P2 | S | — | TODO |
| [014](014-readme-refresh.md) | Make the README describe the app that exists | P2 | S | — | TODO |
| [015](015-route-input-validation.md) | Bound and de-duplicate the API routes' input validation | P2 | M | soft: 010 | TODO |
| [006](006-ticker-override-spike.md) | Design a per-symbol ticker override (**spike**) | P2 | M | — | TODO |
| [007](007-history-caching-spike.md) | Design caching for the price-history route (**spike**) | P2 | M | — | TODO |
| [008](008-export-as-of-timestamp.md) | Capture the export's "As of" timestamp and show file freshness | P2 | S | **004** | TODO |
| [016](016-small-cleanups.md) | Clear the small stuff: misplaced dep, dead vars, a bad edge case | P3 | S | — | TODO |
| [017](017-pdf-code-splitting.md) | Load the PDF stack only when someone exports a PDF | P3 | S | — | TODO |

Status values: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED` (with a one-line
reason) | `REJECTED` (with a one-line rationale — finding fixed independently or
approach abandoned).

Plans 006 and 007 are **design spikes**. Their deliverable is a written design
document, not a feature. They must not modify production code.

## Dependency notes

**Hard dependency**

- **008 requires 004.** Plan 008 bumps `PARSER_VERSION`, which forces every
  persisted source through `parseActivities` on the next page load. Until 004
  lands, a stored file that fails that re-parse is silently deleted from
  IndexedDB — exactly the bug 004 fixes. Running 008 first would trigger the data
  loss for real users. 008 has a STOP condition that greps for 004's function.

**Soft ordering**

- **001 should land before everything else.** Nothing hard-depends on it, but it
  is what makes the other plans' fixes stick: there is currently no CI, so every
  fix here is a one-time patch with nothing preventing regression.
- **009 after 001.** 009 adds a `TZ` pin to `vitest.config.mts`, and that pin is
  what lets CI catch the local-vs-UTC bug class at all. A UTC CI box without it
  gives false confidence on precisely these bugs.
- **015 after 010.** Both edit `src/app/api/prices/history/route.ts`, in
  different functions. Sequencing avoids a conflict.

**Same-file collisions — land sequentially, not in parallel**

- **004 and 005** both edit `hydrate` in `src/stores/dataset.ts`, on adjacent
  lines, for complementary reasons: 004 makes the re-parse write-back
  non-destructive, 005 makes the state merge non-destructive. Both contain a
  reconciliation note for whichever goes second.
- **003 and 008** both add a field to `SourceFile` and `SourceSummary` in
  `src/lib/merge.ts`. They do not conflict logically; both say to preserve the
  other's field if already present.
- **009 and 012** both edit `src/lib/price-snapshot.ts` — different functions
  (`asOf`/`snapshotAgeDays` vs `toNumber`), no line overlap.
- **009 and 015** both edit `src/lib/live-prices.ts` — different functions
  (`snapshotFromLivePrices` vs the extracted validator).
- **002, if landed, is touched by both 003 and 008** — each adds a field to
  `SourceFile`, so `merge.test.ts`'s fixture factory needs the new default.

## Baseline at planning time

Measured at commit `d1d2640`, not assumed. If any of these differs when you
start, treat it as drift and check your plan's "Current state" excerpts before
proceeding.

| Command | Result |
|---|---|
| `pnpm typecheck` | exit 0 |
| `pnpm test` | exit 0 — 13 files, 227 tests |
| `pnpm check` | exit 0 — 5 warnings, all unused variables in `src/lib/google-sheet.ts` |
| `pnpm build` | not measured during planning; documented as clean on `WSA-007` |

`pnpm check` exiting 0 while printing warnings is expected. Only plan 016 takes
that count to zero, and no plan promotes warnings to errors.

## Findings considered and rejected

Recorded so they are not re-audited next time.

**Rejected as incorrect** (subagent findings that did not survive verification):

- *A staged CSV export in `public/`.* There is no `public/` directory in this
  repository at all. The finding was fabricated.
- *The `coverage` object identity defeats the analytics page's memos*
  (`analytics-overview.tsx:144`). It does not. `coverage` resolves to
  `dataset.dateRange`, a stable reference, whenever `dataset` exists; the fresh
  `{ start: "", end: "" }` literal only materialises when `dataset` is null, and
  in that branch the memo short-circuits to an empty `Map`. No expensive work is
  repeated.
- *The "Year to date" filter preset resolves to 31 December of the prior year.*
  It does not. `resolveDateFrom` returns early for `ytd` via pure string
  arithmetic (`dashboard-filters.tsx:32`), never reaching the `Date` path. The
  underlying bug in that function is real but affects only the 30d/3m/6m/12m
  presets, and is fixed by plan 009.
- *Passing a no-op logger to `yahoo-finance2` would stop ticker symbols reaching
  server logs.* It would not. The `console.error(url)` in the library's fetch
  path is not routed through the configurable logger, so a custom logger cannot
  suppress it. Recorded in plan 015 as explicitly out of scope for that reason.

**Rejected as not worth doing:**

- *Decomposing `src/lib/google-sheet.ts` (1366 lines), `src/lib/positions.ts`
  (1008), or `src/lib/metrics.ts`'s derivation core.* Long, but coherent and
  unusually well-commented. Splitting the eight sheet builders would trade a
  readable module for a directory of files sharing an implicit contract about
  anchor rows.
- *Adding jsdom + Testing Library for component tests.* `vitest.config.mts`
  records a deliberate choice ("Every module under test is pure functions over
  plain objects — no DOM"), the suite runs in ~260ms because of it, and the
  untested code that actually endangers money is `merge.ts` and
  `parseActivities`, not JSX. Revisit only if a component bug costs something.
- *An XLSX / Google Sheets **import** to pair with the export.* Looks like a
  missing pair and is not. The round trip that matters — prices back in —
  already exists (`parsePriceCsv`). The workbook's other tabs are derived views
  the app can always recompute, so reimporting them would create a second source
  of truth for numbers the app already owns.
- *A generic multi-broker CSV importer.* The value of this codebase is
  `docs/wealthsimple-csv-format.md` — 530 lines of Wealthsimple-specific rules,
  most of which exist because the intuitive reading is wrong. A generic importer
  dilutes exactly that. A *second broker's* dictionary written to the same
  standard would be real direction; "more formats" would not.
- *The `nanoid` advisory (GHSA-2v37-7h3g-55p8, high).* Reached only via
  `@tailwindcss/postcss > postcss` at build time. The vulnerability needs an
  attacker-controlled `size: 0` passed to `customAlphabet`/`customRandom`;
  nothing here does that. Real runtime impact is nil. Worth clearing on the next
  routine lockfile refresh, not worth a plan — and explicitly out of scope for
  plan 016.
- *Pre-commit hooks and a `CONTRIBUTING.md`.* Solo repository, Biome runs in
  under 30ms across 123 files, and `README.md` already states the fork/branch/PR
  flow.

## Deferred, with the evidence — do not re-derive

Two findings from the original audit were reassessed against the code and
deliberately left unplanned. The evidence below is what changed the assessment.

**Sharing the `buildPositions` walk.** Audited as a MED-impact, S–M-effort win;
it is smaller and more dangerous than that.

- *The trap*: `src/components/charts/capital-chart.tsx:127` calls
  `buildPositions(activities)` **without** `{ sources }`, while
  `src/components/dashboard.tsx:46` calls it **with** `{ sources }` — on the
  **same array identity**, on the same route. `options.sources` is read once
  (`positions.ts:748`) and only affects `historyConfidence`, `historyReasons`
  and `issues`; every numeric field is identical either way. So a
  `WeakMap<Activity[], PositionsReport>` keyed on the array alone would, depending
  on render order, hand the sources-aware callers
  (`investment-overview.tsx:33`, `account-breakdown.tsx:51`) a report with the
  merge-conflict warnings silently missing. Any future attempt must either key on
  the options too, or make capital-chart pass `{ sources }` first — which is a
  five-call-site prop change, since `CapitalChartProps` does not currently carry it.
- *The win is small*: sharing the five component calls saves about four walks.
  Meanwhile `src/lib/price-history.ts:118` calls `buildPositions` once per month
  and `:242` once per year, over growing `slice`/`filter` prefixes — 100+ walks
  that an identity-keyed memo **cannot** help, because each input is a fresh
  array. The component walks are not where the time goes.
- *No precedent exists*: zero derived Zustand selectors, no middleware, no
  `useShallow`, no `src/hooks/` directory, no memo helper in `src/lib/`. The
  repo's comments treat component-level `useMemo` as the deliberate seam
  (`live-prices-card.tsx:29-30`, `capital-chart.tsx:116-117`).

**A Content-Security-Policy.** Audited as S effort; it is not.

- `next-themes` (via `src/components/theme-provider.tsx:9`) injects a **blocking
  inline script** before paint — needs `'unsafe-inline'` or a nonce.
- `src/components/ui/chart.tsx:94-96` injects a `<style>` element via
  `dangerouslySetInnerHTML`, and Recharts adds many more inline styles at
  runtime — so `style-src` needs `'unsafe-inline'` or a nonce-everything approach.
- The PDF path is the one that genuinely needs `data:`/`blob:`:
  `html2canvas-pro` serialises the DOM through `data:`/`blob:` image and
  `foreignObject` loads, and `jsPDF`'s `save()` creates a `blob:` URL. A
  too-strict policy would fail the export into
  `pdf-export-button.tsx`'s generic toast, with the real reason only in the
  console.
- `next.config.ts` has **no `headers()` at all** and there is no
  `src/middleware.ts`, so a nonce-based policy means introducing middleware from
  scratch.
- *What is still cheap and worth doing eventually*: `connect-src 'self'` alone.
  The app makes exactly one `fetch` (`src/lib/live-prices.ts:206`, same-origin),
  fonts are self-hosted by `next/font/google` at build time, and the one external
  URL (`google-sheet.ts:206`) is a link target rather than a fetch. That single
  directive would encode the product's privacy promise in something the browser
  enforces, and nothing in the app conflicts with it.

## Newly found during planning — folded into plan 009

Three date bugs that the original audit missed, all sharing the root cause of
the two it did find:

| Site | Effect |
|---|---|
| `src/lib/price-snapshot.ts:241` | `snapshotAgeDays` derives "today" via UTC, so a snapshot reads a day older than it is in the evening. Drives the stale-price banner in four components — the highest-impact of the five. |
| `src/components/investment/export-sheet-dialog.tsx:98` | `generatedOn` is stamped into the exported sheet via UTC, while the same dialog uses the local-correct `todayStamp()` for the filename. The file name and its contents disagree. |
| `src/lib/projection.ts:161` | `anniversary()` reads a local "now" through `getUTC*`. Label-only — explicitly **out of scope** for 009. |

Also found: **`src/lib/price-snapshot.test.ts:264,268,272` actively mask the
first of these.** They pass fixed UTC instants, whose `toISOString()` is
timezone-independent, so the assertions agree with the bug in every timezone.
Plan 009 rewrites them from local components and requires the executor to
**observe two of them fail** before applying the fix.

And: `vitest.config.mts` pins no `TZ`, so this entire bug class is invisible on a
UTC CI runner. Plan 009 adds the pin.

## Audited but not planned

Verified findings not selected for planning. Real; recorded so nothing has to be
re-derived.

| Finding | Where | Note |
|---|---|---|
| Account IDs in URL paths | `src/app/accounts/[type]/[accountId]/page.tsx` | Reaches browser history and, on a hosted deployment, access logs — including via `<Link>` hover prefetch. Trade-off against deep-linkable account URLs; genuinely the maintainer's call. |
| Storage write ordering | `src/lib/storage.ts:192`, `src/stores/dataset.ts:32` | Two overlapping `saveSources` calls can commit out of order, letting a stale snapshot win. Real but narrow; needs a queue or revision counter. Explicitly deferred out of plan 004. |

## Not audited

- `src/components/ui/**` — vendored shadcn primitives.
- Real bundle output. Plan 017 cites module sizes on disk, and requires the
  executor to measure the actual before/after chunk delta rather than assume it.
